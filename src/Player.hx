import h3d.Vector;
import Entity.PathedEntity;
import h3d.scene.Mesh;

class Player extends PathedEntity {
	public function new(id:Int, mesh:Mesh, world:WorldSquare) {
		super(id, mesh, world);
	}

    //TODO
}
